import javax.swing.*;
import java.awt.*;

public class MachineGunTower extends Tower {
    Image base = new ImageIcon(getClass().getResource("/Defaultsize/towerDefense_tile" + "182" + ".png")).getImage();

    public MachineGunTower(int x, int y) {
        this.x = x;
        this.y = y;
        this.img = new ImageIcon(getClass().getResource("/Defaultsize/towerDefense_tile" + "203" + ".png")).getImage();
        this.dame = 5;
        this.gunspeed = 20;
        this.shootRange = 96;
    }

    @Override
    public void render(Graphics2D graphics2D) {
        super.render(graphics2D);
        int i = (int) x;
        int j = (int) y;
        graphics2D.drawImage(base, i , j, null);
        graphics2D.drawImage(img, i, j, null);
    }
}
